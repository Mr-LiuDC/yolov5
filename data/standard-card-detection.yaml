# star training
# python train.py --epochs 100 --batch-size 8 --weights ./weights/yolov5m.pt --data data/standard-card-detection.yaml --name standard-card-detection

# export
# python export.py --weights runs/train/standard-card-detection/weights/best.pt --include onnx --simplify
# python export.py --opset 12 --int8 --weights runs/train/standard-card-detection/weights/best.pt --include onnx --simplify

# Detect
# python detect.py --weights runs/train/standard-card-detection/weights/best.onnx --dnn --data data/standard-card-detection.yaml


dataset_name: standard-card
path: ../datasets/standard-card
val: yolo_val_list.txt
test: yolo_test_list.txt
train: yolo_train_list.txt

val_rate: 0.2
test_rate: 0.1
train_rate: 0.8

names:
  0: card

# download xml labeled dataset and convert to yolo format
download: |
  import os
  import shutil
  import xml.etree.ElementTree as ET
  from random import shuffle
  
  from utils.general import download, Path
  
  def convert_xml_to_yolo(path, lb_path, ann_name):
      def convert_box(size, box):
          dw, dh = 1. / size[0], 1. / size[1]
          x, y, w, h = (box[0] + box[1]) / 2.0 - 1, (box[2] + box[3]) / 2.0 - 1, box[1] - box[0], box[3] - box[2]
          return x * dw, y * dh, w * dw, h * dh
  
      in_file = open(path / f'Annotations/{ann_name}')
      out_file = open(lb_path, 'w')
      tree = ET.parse(in_file)
      root = tree.getroot()
      size = root.find('size')
      w = int(size.find('width').text)
      h = int(size.find('height').text)
  
      names = list(yaml['names'].values())
      for obj in root.iter('object'):
          cls = obj.find('name').text
          if cls in names and int(obj.find('difficult').text) != 1:
              xmlbox = obj.find('bndbox')
              bb = convert_box((w, h), [float(xmlbox.find(x).text) for x in ('xmin', 'xmax', 'ymin', 'ymax')])
              cls_id = names.index(cls)  # class id
              out_file.write(" ".join([str(a) for a in (cls_id, *bb)]) + '\n')
  
  
  val_rate = yaml['val_rate']
  test_rate = yaml['test_rate']
  train_rate = yaml['train_rate']
  
  data_name = yaml['dataset_name']
  base_dir = Path(yaml['path'])
  url = "https://nutrition-bucket.rplushealth.cn/datasets/"
  urls = [f'{url}{data_name}.zip']
  download(urls, dir=base_dir.parent, delete=False, curl=True, threads=len(urls))
  
  lbs_path = base_dir / 'labels'
  lbs_path.mkdir(exist_ok=True, parents=True)
  
  ann_files = []
  
  for root, dirs, files in os.walk(base_dir / 'Annotations'):
      ann_files = files
  
  for ann_file in ann_files:
      lb_path = lbs_path / (ann_file.replace('.xml', '.txt'))
      convert_xml_to_yolo(base_dir, lb_path, ann_file)
  
      shutil.copytree(base_dir / 'JPEGImages', base_dir / 'images')
      shuffle(ann_files)
      val_files = ann_files[:round(len(ann_files) * val_rate)]
      test_files = ann_files[:round(len(ann_files) * test_rate)]
      train_files = ann_files[round(len(ann_files) * val_rate):]
      open(f'{base_dir}/yolo_val_list.txt', 'w').write("./images/" + "./images/".join([file.replace('.xml', '.jpg') + '\n' for file in val_files]))
      open(f'{base_dir}/yolo_test_list.txt', 'w').write("./images/" + "./images/".join([file.replace('.xml', '.jpg') + '\n' for file in test_files]))
      open(f'{base_dir}/yolo_train_list.txt', 'w').write("./images/" + "./images/".join([file.replace('.xml', '.jpg') + '\n' for file in train_files]))
